import SalesList from './SalesList.tsx'

/** Invoices (PRD 5.17): find an invoice, view it and print it. */
export default function InvoicesPage() {
  return <SalesList key="invoices" mode="invoices" />
}
